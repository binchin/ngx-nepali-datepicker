import { ExtractDateNumberPipe } from './extract-date-number.pipe';

describe('ExtractDateNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractDateNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
